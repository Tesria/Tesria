// Where Tesria itself lives, for the few places the app links to it: the
// "Powered by" line under a branded sign-in and the version line at the foot
// of Administration. Replaced the owner's personal site before the public
// release (dev-plan 14.1).

/** The project's site, with the docs pages. */
export const TESRIA_SITE = 'https://tesria.com'

/** The source code. */
export const TESRIA_SOURCE = 'https://github.com/Tesria/Tesria'

/**
 * Supporting the project (Administration, About). Tesria is free; this is
 * for people who want to say thanks. The page on tesria.com lists the ways
 * (GitHub Sponsors and Ko-fi), so they can change without a release.
 */
export const TESRIA_SUPPORT = 'https://tesria.com/support'
